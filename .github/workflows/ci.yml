name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Setup helmfile
      uses: ./
    - name: Test
      run: |
        kubectl version
        helm version
        helmfile --version
